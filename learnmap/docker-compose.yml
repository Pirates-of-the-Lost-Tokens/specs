services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: learnmap
      POSTGRES_PASSWORD: learnmap
      POSTGRES_DB: learnmap
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
